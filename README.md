TechMill website
===

To setup the dev environment:

    $ ./setup.sh

To run the dev server on port 9000:

    $ ./preview.sh

To build and publish:

    $ ./build.sh
    $ git add build
    $ git commit
